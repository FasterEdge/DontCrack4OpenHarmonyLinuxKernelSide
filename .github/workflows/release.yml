name: '跨平台 Release 打包'
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: '构建 ${{ matrix.target }}'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:

          - { target: openharmony-amd64,     goos: linux,   goarch: amd64, ext: '' }
          - { target: openharmony-arm64,     goos: linux,   goarch: arm64, ext: '' }
          - { target: openharmony-armv7,     goos: linux,   goarch: arm,   ext: '' }
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-go@v5
      with:
        go-version: '1.25'
    - name: '编译 ${{ matrix.target }}'
      run: |
        mkdir -p dist
        CGO_ENABLED=0 GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} \
          go build -trimpath -ldflags="-s -w" -o dist/dontcrack-openharmony-linux-kernel${{ matrix.ext }} .
    - name: '打包'
      run: |
        cd dist
        if [ "${{ matrix.goos }}" = "windows" ]; then
          zip -qr ../dontcrack-openharmony-linux-kernel-${{ matrix.target }}.zip dontcrack-openharmony-linux-kernel.exe
        else
          tar czf ../dontcrack-openharmony-linux-kernel-${{ matrix.target }}.tar.gz dontcrack-openharmony-linux-kernel
        fi
    - uses: actions/upload-artifact@v4
      with:
        name: 'dontcrack-openharmony-linux-kernel-${{ matrix.target }}'
        path: '${{ github.workspace }}/dontcrack-openharmony-linux-kernel-${{ matrix.target }}.*'
        if-no-files-found: 'error'
        retention-days: 1

  release:
    name: '发布 GitHub Release'
    needs: build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/download-artifact@v4
      with:
        path: artifacts
        merge-multiple: true
    - name: '整理产物'
      run: |
        mkdir -p release
        find artifacts -type f | while read -r f; do
          case "$f" in
            *.tar.gz|*.zip) cp "$f" release/ ;;
          esac
        done
    - name: '上传到 Release'
      uses: softprops/action-gh-release@v2
      with:
        files: 'release/*'
        generate_release_notes: true
        fail_on_unmatched_files: false
